#include <bits/stdc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int T; cin>>T;
	while(T--){
		int n;
		vector<int> v(n);
		for (int i=0; i<n; i++) cin>>v[i];
		bool left = false;
		bool right = false;
		int ans = 0;
		int current_length = 0;
		for (int i=0; i<n; i++){

		}
	}
}