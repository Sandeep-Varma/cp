#include <bits/stdc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int T; cin>>T;
	while(T--){
		int n,m; cin>>n>>m;
		vector<int> t(m);
		for(int i=0; i<m; i++) cin>>t[i];
		vector<int> c(m);
		for(int i=0; i<m; i++) cin>>c[i];
	}
}